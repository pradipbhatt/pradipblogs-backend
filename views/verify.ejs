<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Email Verification</title>
    <style>
        body {
            background-color: #000;
            color: #fff;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: Arial, sans-serif;
        }
        .container {
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 8px;
            width: 300px;
            text-align: center;
        }
        .btn {
            display: inline-block;
            padding: 10px 15px;
            margin-top: 15px;
            border-radius: 4px;
            background: #634da3;
            color: #fff;
            text-decoration: none;
            text-align: center;
        }
        .btn:hover {
            background: #4e3b82;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Email Verification</h1>
        <p><%= message %></p>
        <a href="https://pradipblogs.vercel.app//login" class="btn">Login Now</a> <!-- Change '/login' to the actual login route -->
    </div>
</body>
</html>
